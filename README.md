**Fashion-MNIST Image Classification using CNN**

This notebook builds a CNN model to classify clothing images from the Fashion-MNIST dataset into different fashion categories.

**Dataset**

Fashion-MNIST
28×28 grayscale images
10 fashion classes (shirts, shoes, bags, etc.)

**Model Used** - Convolutional Neural Network (CNN)

**Tools** - Python, TensorFlow/Keras, NumPy, Matplotlib

**Purpose** - Apply CNNs to fashion image classification
